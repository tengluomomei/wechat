<view class="h-tit">
  <text class="usermotto">title组件</text>
  <view>{{msg}} - {{count}}</view>
  <button bindtap="testFun">执行test函数</button>
  <view class="content">
    <!-- <slot>这是默认插槽</slot> -->
    <slot name="left"></slot>
    <slot name="right"></slot>
  </view>
  <view class="default">这是默认插槽(1)</view>
</view>

